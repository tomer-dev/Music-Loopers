//require('normalize.css/normalize.css');
//require('styles/TrackMenu.css');

import React from 'react';
import App from './App';

class AppComponent extends React.Component {
    render() {
        return (<App/>
        );
    }
}

AppComponent.defaultProps = {
};

export default AppComponent;
